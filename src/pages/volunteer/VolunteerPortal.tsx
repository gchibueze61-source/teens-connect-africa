import { resources } from '../../data/content';import { Placeholder } from '../../components/Placeholder';
export function VolunteerPortal(){return <section className="mx-auto max-w-7xl px-4 py-16"><h1 className="text-4xl font-black">Volunteer Dashboard</h1><p className="mt-2">Announcements, approval status, profile, notifications, tasks, attendance, certificates and shared resources.</p><div className="mt-6 grid gap-6 md:grid-cols-3">{resources.map(r=><div className="rounded-3xl border p-6 dark:border-slate-800" key={r.id}><h2 className="font-bold">{r.title}</h2><p>{r.type} • {r.audience}</p><button className="mt-4 rounded-xl bg-orange-600 px-4 py-2 text-white">Download</button></div>)}</div><Placeholder label="UPLOAD VOLUNTEER PHOTOS / CERTIFICATES / TRAINING MEDIA" className="mt-8"/></section>}
