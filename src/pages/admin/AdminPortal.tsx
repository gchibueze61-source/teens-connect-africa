import { collections, storageFolders } from '../../lib/firestoreSchema';import { Placeholder } from '../../components/Placeholder';
export function AdminPortal(){return <section className="mx-auto max-w-7xl px-4 py-16"><h1 className="text-4xl font-black">Founder / Administrator Portal</h1><p className="mt-2">Full CMS and operations dashboard for approvals, CRUD, reports, analytics, roles and Firebase data.</p><div className="mt-6 grid gap-4 md:grid-cols-3">{collections.map(c=><div className="rounded-2xl border p-4 dark:border-slate-800" key={c}><b>{c}</b><p className="text-sm text-slate-500">Create • edit • archive • delete • publish where applicable</p></div>)}</div><h2 className="mt-10 text-2xl font-black">Media Library Upload Areas</h2><div className="mt-4 grid gap-4 md:grid-cols-3">{storageFolders.map(f=><Placeholder key={f} label={`UPLOAD ${f.toUpperCase().replaceAll('-',' ')}`}/>)}</div></section>}
