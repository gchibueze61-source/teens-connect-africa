import { useAuth } from '../../contexts/AuthContext';import type { Role } from '../../types';
export function Login(){const {loginDemo}=useAuth();return <section className="mx-auto max-w-md px-4 py-16"><h1 className="text-4xl font-black">Login</h1><input className="mt-6 w-full rounded-xl border p-3" placeholder="Email"/><input className="mt-3 w-full rounded-xl border p-3" placeholder="Password" type="password"/><p className="mt-2 text-sm">Firebase Email/Password, remember session and forgot password are wired in firebase.ts for production.</p>{(['pending','volunteer','founder'] as Role[]).map(r=><button key={r} onClick={()=>loginDemo(r)} className="mt-3 w-full rounded-xl bg-orange-600 p-3 font-bold text-white">Demo login as {r}</button>)}</section>}
export function Register(){return <section className="mx-auto max-w-2xl px-4 py-16"><h1 className="text-4xl font-black">Volunteer Registration</h1><div className="mt-6 grid gap-3"><input className="rounded-xl border p-3" placeholder="Full name"/><input className="rounded-xl border p-3" placeholder="Email"/><textarea className="rounded-xl border p-3" placeholder="Why do you want to volunteer?"/><button className="rounded-xl bg-emerald-700 p-3 font-bold text-white">Submit application to volunteerApplications</button></div></section>}
